"""Setup script for hash_operations package."""

from setuptools import setup, find_packages

setup(
    name='hash_operations',
    version='1.0.0',
    description='Hash operations utility for CloudFormation deployments',
    py_modules=['hash_operations'],
    install_requires=[],
    python_requires='>=3.8'
)
